{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/nodes/motifs.base.json",
  "type": "object",
  "properties": {
    "tf_name": {
      "type": "string",
      "description": "Name of the transcription factor whose binding preference the motif describes, in the form the source uses: HOCOMOCO appends a species suffix, while SEMpl carries the bare HGNC symbol, colon-joined for a complex.",
      "example": "ATF1_HUMAN"
    },
    "pwm": {
      "type": "array",
      "description": "Position weight matrix for the motif: an N-by-4 matrix with one row per motif position and one score per nucleotide. Scores are stored as strings.",
      "items": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "length": {
      "type": "number",
      "description": "Number of positions in the motif, equal to the number of rows in pwm."
    }
  },
  "required": [
    "tf_name",
    "pwm",
    "length"
  ],
  "db_collection_name": "motifs"
}
