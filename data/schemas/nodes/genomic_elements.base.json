{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schemas/nodes/genomic_elements.base.json",
  "type": "object",
  "properties": {
    "chr": {
      "type": "string",
      "description": "Chromosome the element lies on, prefixed with 'chr'.",
      "example": "chr1"
    },
    "start": {
      "type": "number",
      "description": "Start of the element, 0-based and inclusive."
    },
    "end": {
      "type": "number",
      "description": "End of the element, 0-based and exclusive."
    },
    "strand": {
      "type": "string",
      "description": "Strand the element is annotated on: + for forward, - for reverse, or . where strand does not apply."
    },
    "type": {
      "type": "string",
      "description": "Category of element.",
      "example": "tested elements"
    },
    "method": {
      "type": "string",
      "description": "The assay or pipeline that defined the element.",
      "example": "MPRA"
    }
  },
  "required": [
    "chr",
    "start",
    "end",
    "type",
    "method"
  ],
  "accessible_via": {
    "return": "_id, name, chr, start, end, strand, type, source_annotation, source, source_url"
  },
  "db_collection_name": "genomic_elements"
}
