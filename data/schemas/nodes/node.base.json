{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "node.base.json",
  "title": "Base Node Schema",
  "description": "Base schema for all node types in the IGVF Catalog. Defines common required properties.",
  "allOf": [
    {
      "properties": {
        "source_url": {
          "$ref": "../mixins.json#/source_url"
        }
      }
    },
    {
      "type": "object",
      "properties": {
        "_key": {
          "type": "string",
          "description": "Unique identifier for this record within its collection. Key composition is collection-specific: a stable source accession where one exists, otherwise a composite of the fields that make the record unique."
        },
        "name": {
          "type": "string",
          "description": "Human readable display name for the entity",
          "example": "TP53"
        },
        "source": {
          "$ref": "../mixins.json#/source",
          "example": "IGVF"
        }
      },
      "required": [
        "_key",
        "name",
        "source",
        "source_url"
      ]
    }
  ]
}
