{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/nodes/motifs.Motif.json",
  "allOf": [
    {
      "$ref": "node.base.json"
    },
    {
      "$ref": "motifs.base.json"
    },
    {
      "type": "object",
      "properties": {
        "source": {
          "enum": [
            "HOCOMOCOv11"
          ]
        },
        "source_url": {
          "pattern": "^hocomoco11\\.autosome\\.org/motif/.*$",
          "example": "hocomoco11.autosome.org/motif/ATF1_HUMAN.H11MO.0.B",
          "description": "HOCOMOCO v11 motif page for this model, as host and path with no URL scheme."
        },
        "class": {
          "type": "string",
          "description": "Evidence tier for the underlying data, taken from the associated files_filesets record: 'observed data' for a motif derived from ChIP-seq.",
          "enum": [
            "observed data"
          ]
        },
        "method": {
          "type": "string",
          "description": "Model collection that produced this motif, taken from the associated files_filesets record.",
          "enum": [
            "HOCOMOCO"
          ]
        },
        "files_filesets": {
          "type": "string",
          "description": "Handle of the files_filesets record for the IGVF file the HOCOMOCO PWM bundle was read from.",
          "example": "files_filesets/IGVFFI1444TRQL",
          "pattern": "^files_filesets/",
          "collections": [
            "files_filesets"
          ]
        }
      },
      "required": [
        "class",
        "method",
        "files_filesets"
      ],
      "additionalProperties": false,
      "accessible_via": {
        "return": "_id, name, tf_name, source, source_url, pwm, length, class, method, files_filesets"
      }
    }
  ],
  "db_collection_name": "motifs"
}
