{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schemas/edges/variants_genes.AFGRSQtl.json",
  "allOf": [
    {
      "$ref": "edges.base.json"
    },
    {
      "type": "object",
      "properties": {
        "_key": {
          "type": "string",
          "description": "SHA-256 hash of the variant key, Ensembl gene ID and dataset identifier."
        },
        "_from": {
          "example": "variants/NC_000001.11:10000:T:A",
          "pattern": "^variants/",
          "collections": [
            "variants"
          ]
        },
        "_to": {
          "example": "genes/ENSG00000187583",
          "pattern": "^genes/ENSG[0-9]{11}(?:_PAR_Y)?$",
          "collections": [
            "genes"
          ]
        },
        "biological_context": {
          "type": "string",
          "description": "Biological context for the variant to gene edge",
          "enum": [
            "lymphoblastoid cell line"
          ]
        },
        "biosample_term": {
          "type": "string",
          "description": "Biosample term for the variant to gene edge",
          "enum": [
            "ontology_terms/EFO_0005292"
          ]
        },
        "chr": {
          "type": "string",
          "description": "Chromosome for the variant to gene edge",
          "example": "chr1",
          "pattern": "^chr"
        },
        "neg_log10_pvalue": {
          "type": "number",
          "description": "Negative log10 of the p-value for the variant to gene edge"
        },
        "p_value": {
          "type": "number",
          "description": "Pvalue for the variant to gene edge"
        },
        "effect_size": {
          "type": "number",
          "description": "Effect size for the variant to gene edge"
        },
        "posterior_inclusion_probability": {
          "type": ["number", "null"],
          "description": "Posterior inclusion probability for the variant to gene edge"
        },
        "log2FC": {
          "type": ["number", "null"],
          "description": "Log2 fold change for the variant to gene edge"
        },
        "significant": {
          "type": "boolean",
          "description": "Whether the variant-gene association is statistically significant"
        },
        "class": {
          "enum": [
            "observed data"
          ]
        },
        "method": {
          "enum": [
            "spliceQTL"
          ]
        },
        "label": {
          "enum": [
            "spliceQTL"
          ]
        },
        "intron_chr": {
          "type": "string",
          "description": "Intron chromosome for the variant to gene edge",
          "example": "chr1",
          "pattern": "^chr"
        },
        "intron_start": {
          "type": "string",
          "description": "Intron start for the variant to gene edge"
        },
        "intron_end": {
          "type": "string",
          "description": "Intron end for the variant to gene edge"
        },
        "source": {
          "enum": [
            "AFGR"
          ]
        },
        "source_url": {
          "enum": [
            "https://github.com/smontgomlab/AFGR"
          ]
        },
        "files_filesets": {
          "type": "string",
          "description": "The filesets of the variant to gene edge"
        },
        "name": {
          "enum": [
            "modulates splicing of"
          ]
        },
        "inverse_name": {
          "enum": [
            "splicing modulated by"
          ]
        },
        "biological_process": {
          "type": "string",
          "description": "Biological process for the variant to gene edge",
          "enum": [
            "ontology_terms/GO_0043484"
          ]
        }
      },
      "required": [
        "_key",
        "chr",
        "neg_log10_pvalue",
        "p_value",
        "effect_size",
        "biological_context",
        "biosample_term",
        "biological_process",
        "intron_chr",
        "intron_start",
        "intron_end",
        "files_filesets"
      ],
      "additionalProperties": false
    }
  ],
  "accessible_via": {
    "filter_by_range": "intron_start, intron_end, neg_log10_pvalue, effect_size, posterior_inclusion_probability, log2FC",
    "simplified_return": "class, method, label, source, neg_log10_pvalue, biological_context",
    "return": "intron_chr, intron_start, intron_end, effect_size, neg_log10_pvalue, source, class, method, label, p_value, posterior_inclusion_probability, log2FC, chr, source_url, files_filesets, biological_context, biosample_term"
  },
  "db_collection_name": "variants_genes"
}
