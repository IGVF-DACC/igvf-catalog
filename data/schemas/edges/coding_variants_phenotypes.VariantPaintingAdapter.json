{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schemas/edges/coding_variants_phenotypes.VariantPaintingAdapter.json",
  "allOf": [
    {
      "$ref": "edges.base.json"
    },
    {
      "type": "object",
      "properties": {
        "_key": {
          "type": "string",
          "description": "Coding variant key, phenotype ontology term and source file accession, joined by underscores."
        },
        "_from": {
          "example": "coding_variants/coding_var_1",
          "pattern": "^coding_variants/",
          "collections": [
            "coding_variants"
          ]
        },
        "_to": {
          "example": "ontology_terms/phenotype_1",
          "pattern": "^ontology_terms/",
          "collections": [
            "ontology_terms"
          ]
        },
        "source": {
          "description": "The database, dataset or organization that is a source for these data.",
          "enum": [
            "IGVF"
          ]
        },
        "source_url": {
          "description": "The URL for the source.",
          "example": "https://data.igvf.org/tabular-files/IGVFFI9499PJFU",
          "pattern": "^https://data.igvf.org/tabular-files/"
        },
        "name": {
          "description": "Directional human readable label for the relationship, _from to _to.",
          "enum": [
            "mutational effect"
          ]
        },
        "inverse_name": {
          "description": "Reversed directional human readable label for the relationship, _to to _from.",
          "enum": [
            "altered due to mutation"
          ]
        },
        "files_filesets": {
          "type": "string",
          "description": "Reference to the File and Metadata",
          "example": "files_filesets/IGVFFI9499PJFU",
          "pattern": "^files_filesets/",
          "collections": [
            "files_filesets"
          ]
        },
        "biological_context": {
          "type": "string",
          "description": "Plain-language summary of the sample the assay was run in, taken from the associated files_filesets record.",
          "example": "U2OS"
        },
        "biosample_term": {
          "type": "string",
          "description": "Ontology term for the experimental sample, or the sample the prediction is valid for",
          "example": "ontology_terms/EFO_0002869"
        },
        "method": {
          "description": "Name of the experimental class or prediction model that produced this result.",
          "enum": [
            "Variant painting via fluorescence"
          ]
        },
        "label": {
          "description": "Non-method specific human readable label for the node or edge",
          "enum": [
            "protein variant effect"
          ]
        },
        "class": {
          "description": "The class of the data this record belongs to: 'observed data' for a direct experimental measurement, 'prediction' for the output of a computational model, and 'biological relationship' for a curated relationship.",
          "enum": [
            "observed data"
          ]
        },
        "localization_score": {
          "type": "number",
          "description": "XGBoost classifier score measuring the probability of protein mislocalization caused by the variant"
        },
        "mislocalization_hit": {
          "type": "boolean",
          "description": "Whether the variant is classified as causing protein mislocalization based on the localization score threshold"
        }
      },
      "required": [
        "_key",
        "files_filesets",
        "biosample_term",
        "biological_context",
        "localization_score",
        "mislocalization_hit"
      ],
      "additionalProperties": false
    }
  ],
  "db_collection_name": "coding_variants_phenotypes"
}
