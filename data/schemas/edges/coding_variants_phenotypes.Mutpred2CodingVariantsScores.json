{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schemas/edges/coding_variants_phenotypes.Mutpred2CodingVariantsScores.json",
  "allOf": [
    {
      "$ref": "edges.base.json"
    },
    {
      "type": "object",
      "properties": {
        "_key": {
          "type": "string",
          "description": "Coding variant key, phenotype ontology term and source file accession, joined by underscores."
        },
        "_from": {
          "pattern": "^coding_variants/",
          "collections": [
            "coding_variants"
          ]
        },
        "_to": {
          "pattern": "^ontology_terms/",
          "collections": [
            "ontology_terms"
          ]
        },
        "name": {
          "enum": [
            "mutational effect"
          ]
        },
        "inverse_name": {
          "enum": [
            "altered due to mutation"
          ]
        },
        "pathogenicity_score": {
          "type": "number",
          "description": "MutPred2 pathogenicity score for the substitution, between 0 and 1. Higher values indicate greater predicted pathogenicity."
        },
        "property_scores": {
          "type": "array",
          "description": "Molecular mechanisms MutPred2 predicts the substitution to disrupt, drawn from its 58 structural, functional, and post-translational-modification properties. Only mechanisms reaching a posterior probability of 0.25 or higher with a p-value below 0.05 are retained. Each entry carries the property, its posterior probability, p-value, affected position, and whether the property is Lost, Gained, or Altered; the affected position is \"-\" for properties that describe a whole region rather than a residue.",
          "items": {
            "type": "object"
          }
        },
        "files_filesets": {
          "type": "string",
          "description": "Handle of the files_filesets record for the IGVF file this measurement was read from.",
          "pattern": "^files_filesets/"
        },
        "biosample_term": {
          "type": ["string", "null"],
          "description": "Ontology term for the sample the assay was run in, taken from the file's fileset metadata. Null when the fileset lists no sample.",
          "pattern": "^ontology_terms/"
        },
        "biological_context": {
          "type": ["string", "null"],
          "description": "Plain-language summary of the sample the assay was run in, taken from the file's fileset metadata. Null when the fileset carries no summary.",
          "example": "glutamatergic neuron differentiated cell specimen, pooled cell"
        },
        "method": {
          "enum": [
            "MutPred2"
          ]
        },
        "class": {
          "enum": [
            "prediction"
          ]
        },
        "label": {
          "enum": [
            "predicted protein variant effect"
          ]
        },
        "source": {
          "enum": [
            "IGVF"
          ]
        },
        "source_url": {
          "enum": [
            "https://data.igvf.org/tabular-files/IGVFFI6893ZOAA"
          ]
        }
      },
      "required": [
        "_key",
        "pathogenicity_score",
        "property_scores",
        "files_filesets",
        "biosample_term",
        "biological_context"

      ],
      "additionalProperties": false
    }
  ],
  "db_collection_name": "coding_variants_phenotypes"
}
