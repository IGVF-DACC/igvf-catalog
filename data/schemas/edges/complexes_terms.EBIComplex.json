{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schemas/edges/complexes_terms.EBIComplex.json",
  "type": "object",
  "properties": {
    "_key": {
      "type": "string",
      "description": "Complex accession and ontology term key joined by an underscore.",
      "example": "CPX-1_GO_0071144"
    },
    "_from": {
      "type": "string",
      "description": "Document handle of the source node, in collection/key form.",
      "example": "complexes/CPX-1",
      "pattern": "^complexes/",
      "collections": [
        "complexes"
      ]
    },
    "_to": {
      "type": "string",
      "description": "Document handle of the target node, in collection/key form.",
      "example": "ontology_terms/GO_0071144",
      "pattern": "^ontology_terms/",
      "collections": [
        "ontology_terms"
      ]
    },
    "term_name": {
      "type": "string",
      "description": "Name of the Gene Ontology term, taken from the GO annotation column. Absent on edges built from non-GO cross-references.",
      "example": "heteromeric SMAD protein complex"
    },
    "source": {
      "type": "string",
      "description": "The dataset or consortium this record was loaded from.",
      "enum": [
        "EBI"
      ]
    },
    "source_url": {
      "type": "string",
      "description": "Link to the specific upstream record or file this data was loaded from.",
      "enum": [
        "https://www.ebi.ac.uk/complexportal/"
      ]
    },
    "name": {
      "type": "string",
      "description": "Predicate for the relationship, chosen from the ontology aspect: 'is located in' for cellular component, 'involved in' for biological process, 'has the function' for molecular function, and 'associated with' for disease or other cross-references.",
      "enum": [
        "associated with",
        "is located in",
        "involved in",
        "has the function"
      ]
    },
    "inverse_name": {
      "type": "string",
      "description": "Predicate describing the same relationship in the _to to _from direction.",
      "enum": [
        "associated with",
        "contains",
        "has component",
        "is a function of"
      ]
    },
    "class": {
      "type": "string",
      "description": "Evidence tier for the underlying data, taken from the associated files_filesets record."
    },
    "method": {
      "type": [
        "string",
        "null"
      ],
      "description": "Model or pipeline that produced this record, taken from the associated files_filesets record."
    },
    "label": {
      "type": [
        "string",
        "null"
      ],
      "description": "Same value as method."
    },
    "files_filesets": {
      "type": "string",
      "description": "Handle of the files_filesets record for the IGVF file this record was read from.",
      "example": "files_filesets/IGVFFI1444TRQL",
      "pattern": "^files_filesets/",
      "collections": [
        "files_filesets"
      ]
    }
  },
  "required": [
    "_key",
    "_from",
    "_to",
    "source",
    "source_url",
    "name",
    "inverse_name",
    "class",
    "method",
    "label",
    "files_filesets"
  ],
  "additionalProperties": false,
  "db_collection_name": "complexes_terms"
}
