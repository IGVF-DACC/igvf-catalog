{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schemas/edges/variants_genomic_elements.CAQtl.json",
  "allOf": [
    {
      "$ref": "edges.base.json"
    },
    {
      "type": "object",
      "properties": {
        "_key": {
          "type": "string",
          "description": "Variant SPDI, genomic element key, source-file cell label and source file accession joined by underscores."
        },
        "_from": {
          "example": "variants/NC_000001.11:766454:T:C",
          "pattern": "^variants/",
          "collections": [
            "variants"
          ]
        },
        "_to": {
          "example": "genomic_elements/accessible_dna_element_chr1_778381_779150_GRCh38_ENCFF103XRK",
          "pattern": "^genomic_elements/",
          "collections": [
            "genomic_elements"
          ]
        },
        "rsid": {
          "type": "string",
          "description": "dbSNP id for the variant position",
          "example": "rs189800799"
        },
        "label": {
          "description": "Non-method specific human readable label for the node or edge",
          "enum": [
            "caQTL"
          ]
        },
        "method": {
          "description": "Name of the experimental class or prediction model that produced this result.",
          "enum": [
            "caQTL"
          ]
        },
        "class": {
          "description": "The class of the data this record belongs to: 'observed data' for a direct experimental measurement, 'prediction' for the output of a computational model, and 'biological relationship' for a curated relationship.",
          "enum": [
            "observed data"
          ]
        },
        "source": {
          "description": "The database, dataset or organization that is a source for these data.",
          "enum": [
            "ENCODE"
          ]
        },
        "source_url": {
          "description": "The URL for the source.",
          "example": "https://www.encodeproject.org/files/ENCFF103XRK/",
          "pattern": "^https://www.encodeproject.org/files/"
        },
        "files_filesets": {
          "type": "string",
          "description": "Reference to the File and Metadata",
          "example": "files_filesets/ENCFF103XRK",
          "pattern": "^files_filesets/"
        },
        "biological_context": {
          "type": "string",
          "description": "Plain-language summary of the sample the assay was run in, taken from the associated files_filesets record.",
          "example": "neural progenitor cell"
        },
        "biosample_term": {
          "type": "string",
          "description": "Ontology term for the experimental sample, or the sample the prediction is valid for",
          "pattern": "^ontology_terms/",
          "example": "ontology_terms/CL_0011020"
        },
        "name": {
          "description": "Directional human readable label for the relationship, _from to _to.",
          "enum": [
            "modulates accessibility of"
          ]
        },
        "inverse_name": {
          "description": "Reversed directional human readable label for the relationship, _to to _from.",
          "enum": [
            "accessibility modulated by"
          ]
        },
        "method_term": {
          "type": "string",
          "description": "Ontology term for the chromatin accessibility assay.",
          "enum": [
            "ontology_terms/BAO_0040027"
          ]
        }
      },
      "required": [
        "_key",
        "rsid",
        "files_filesets",
        "biological_context",
        "biosample_term",
        "method_term"
      ],
      "additionalProperties": false
    }
  ],
  "db_collection_name": "variants_genomic_elements"
}
