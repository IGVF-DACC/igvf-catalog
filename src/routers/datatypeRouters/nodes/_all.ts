import { proteins, proteinID } from './proteins'
import { accessibleDnaRegions, accessibleDnaRegionID } from './accessibleDnaRegions'
import { ccres, ccreID } from './ccres'
import { genes, geneID } from './genes'
import { cloOntology, cloOntologyID, uberonOntology, uberonOntologyID, clOntology, clOntologyID, hpoOntology, hpoOntologyID, mondoOntology, mondoOntologyID, goBPOntology, goBPOntologyID, goCCOntology, goCCOntologyID, goMLOntology, goMLOntologyID } from './ontologies'
import { transcripts, transcriptID } from './transcripts'
import { variants, variantID } from './variants'

export const nodeRouters = {
  accessibleDnaRegions,
  accessibleDnaRegionID,
  ccres,
  ccreID,
  proteins,
  proteinID,
  genes,
  geneID,
  cloOntology,
  cloOntologyID,
  uberonOntology,
  uberonOntologyID,
  clOntology,
  clOntologyID,
  hpoOntology,
  hpoOntologyID,
  mondoOntology,
  mondoOntologyID,
  goBPOntology,
  goBPOntologyID,
  goCCOntology,
  goCCOntologyID,
  goMLOntology,
  goMLOntologyID,
  transcripts,
  transcriptID,
  variants,
  variantID
}
